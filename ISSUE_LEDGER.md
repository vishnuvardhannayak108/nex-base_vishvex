# NexBase — Issue Ledger

Every issue verified against the repository before a fix was written. Baseline
captured before any change; see the bottom for before/after.

Risk key: **C** critical · **H** high · **M** medium · **L** low

| ID | File:line (pre-fix) | Verified evidence | Risk | Fix | Test |
|---|---|---|---|---|---|
| NB-01 | `pipeline/profile.py:264` | `industry_for_title('Claims Adjuster') == 'Manufacturing'`; an insurer with observed industry `Insurance` qualified at score 83 as Manufacturing | C | Observed industry wins outright; title inference is `INFERRED_HINT` and never sets `industry_known` | `test_observed_industry_is_never_overwritten_by_title_inference`, `test_production_supervisor_does_not_make_a_company_manufacturing`, `test_inferred_industry_earns_no_bonus_and_keeps_the_review_flag` |
| NB-02 | `db/schema.sql` (whole file) | `grep -ci "row level security"` → `0` across 13 tables | C | RLS enabled + forced on all 13 tables, no permissive policy | `test_every_table_has_row_level_security_enabled` |
| NB-03 | `config.py:59`, `.env:30` | `EMPLOYEE_SIZE_MIN=99` → `size_filter_min == 11` (silently discarded) | C | `AliasChoices` accepts both spellings; `.env.example` corrected | `test_both_size_env_names_configure_the_same_band` |
| NB-04 | `pipeline/qualification.py:260` | Branch unreachable: `industry_known` was only ever true when `client_industry` was set. Coverage showed line 261 never executed | H | Rejection is now driven by `discovery_excluded_sectors` alone | `test_industry_not_relevant_fires_only_on_a_configured_exclusion`, `test_an_observed_industry_outside_the_ten_is_not_a_rejection` |
| NB-05 | `pipeline/runner.py:726` | Second pass forced `REJECTED` for OVERSIZE, ignoring `allow_oversize_with_approval=True` which the gate honours | H | Both passes follow the configured policy | `test_oversize_follows_the_configured_approval_policy`, `test_oversize_is_rejected_when_approval_is_disabled` |
| NB-06 | `pipeline/runner.py:683` | `_resolve_domain` had one call site, inside `_resolve_sizes`, gated on `NEEDS_REVIEW + EMPLOYEE_SIZE_UNKNOWN`; result reached no other stage | H | `_resolve_domains` is a stage; `domain_for()` feeds contacts and enrichment | `test_resolved_domain_reaches_contact_discovery` |
| NB-07 | `pipeline/dedupe.py:143` | 3 domainless "Summit Construction" jobs in CO/FL/ID → 1 company, `hiring_intensity=3`, location `Denver, CO` | H | Domainless sightings need geography corroboration | `test_same_name_different_states_do_not_merge`, `test_same_name_same_state_still_merges_across_sources` |
| NB-08 | `pipeline/dedupe.py:204` | 3 jobs with `REQ-1/2/3`, same title+location → 1 job kept | H | Requisitions resolved per source by id; cross-posts still collapse | `test_distinct_requisitions_are_preserved`, `test_the_same_job_cross_posted_still_collapses` |
| NB-09 | `access/fetcher.py:83` | robots handled only in `site_evidence.py`, reachable only via the size resolver, which is off by default → robots consulted nowhere | H | robots check moved into the shared `AccessLayer.fetch` | `test_access_layer_honours_robots_for_every_fetch`, `test_robots_is_fetched_once_per_host` |
| NB-10 | `api/main.py:43` | `require_api_key` returned early when `NEXBASE_API_KEY` unset; `.env` does not set it | H | Fails closed outside development; constant-time compare | `test_api_auth_fails_closed_outside_development`, `test_api_auth_rejects_a_wrong_key` |
| NB-11 | `api/main.py:129,87,146` | `/leads`, `/stats`, `/approvals` had no auth dependency at all | H | All read endpoints carry `require_api_key` | `test_read_endpoints_require_authentication` |
| NB-12 | `db/repository.py:207` + `outreach/suppression.py:43` | `select` returns `[]` on any read error → empty suppression list → opted-out addresses queueable | H | `list_suppressions` reads strict and raises | `test_suppression_read_failure_fails_closed` |
| NB-13 | `pipeline/runner.py:595` | ATS called with only `ats=`/`limit=`; no query, location, tier or country | M | ATS runs per planned probe with its term and location, bounded by `ats_max_probes` | `test_ats_receives_the_planned_query_and_location`, `test_ats_probe_count_is_bounded` |
| NB-14 | `discovery/ats_discovery.py:222` | `resolve_company_sites` never called; every ATS row had `company_website=None` | M | Called after ATS discovery; only URLs normalising to a real employer domain are attached | covered by `test_ats_receives_the_planned_query_and_location` stub path |
| NB-15 | `runner.py:565` + `jobspy_discovery.py:139` | N planned `(term, location)` pairs re-expanded to `terms × locations` per source | M | Probes executed one at a time, one audit row each | `test_planned_probes_execute_exactly_once_each` |
| NB-16 | `jobspy_discovery.py:148` | All 5 sites in one `scrape_jobs` call; one raise discarded Indeed + LinkedIn too | M | One call per site, per-site status recorded | `test_one_broken_jobspy_site_does_not_discard_the_others` |
| NB-17 | `runner.py:520` | Board default list named the disabled `monster` | M | Default comes from `board_default_sites` | `test_board_default_never_names_a_disabled_adapter` |
| NB-18 | `access/fetcher.py:83` | No scheme/host validation; scraped `company_website` fetched verbatim; response stored as evidence | M | `access/urlguard.py` blocks non-public addresses, bad schemes, credentials | 15 cases in `test_ssrf_guard_rejects`, plus rebinding and layer-level tests |
| NB-19 | `api/main.py:192` | `entry['email']` (originally scraped HTML) interpolated raw into a public `HTMLResponse` | M | `html.escape` on both values | `test_unsubscribe_page_escapes_database_values` |
| NB-20 | `pipeline/normalize.py:95` | Hand-written TLD allowlist silently discarded `.tools`, `.builders`, `.farm`, `.coop` … | M | Structural validation + explicit corrupted-host rejection | `test_modern_employer_tlds_are_kept`, `test_corrupted_hosts_are_rejected` |
| NB-21 | `core/models.py` (all adapters) | An explicit address in an ATS or board description was discarded — only JobSpy has an `emails` column | M | `RawJob.__post_init__` harvests description addresses for every source | `test_ats_description_email_reaches_observed_emails`, `test_board_description_email_reaches_observed_emails`, `test_emails_survive_normalization_and_dedup` |
| NB-22 | `contacts/extraction.py:63` | `hrservicecenter@lowes.com` classified as a person | M | Role list expanded plus substring tokens | `test_role_mailbox_classification` |
| NB-23 | `runner.py:1002` | Page-only emails appeared in `lead.emails` and were then discarded | M | Recorded in the existing `evidence` table with provenance and type; never outreach-eligible | `test_stop_at_before_enrichment_runs_contacts_but_never_enriches` |
| NB-24 | `email/verification.py:77` | ZeroBounce key sent as a URL query parameter | M | POST with `Authorization` header | `test_zerobounce_key_never_travels_in_the_url` |
| NB-25 | `dashboard/app.py:18` | No auth; holds the service-role key; renders contacts and suppression list | M | Optional password gate + documented localhost binding | manual (Streamlit module not import-testable) |
| NB-26 | `discovery/taxonomy.py:26` | `EXCLUDED_SOC_MAJOR_GROUPS` hardcoded at load, ignoring `settings.excluded_soc_majors` | L | Config exclusions applied at sampling; `15-0000` remains the floor | `test_computer_occupations_stay_excluded_even_with_empty_config` |
| NB-27 | `discovery/planner.py:22` | `SECONDARY_INDUSTRIES` was a literal, so core allocation bypassed the taxonomy | L | Derived from `taxonomy.core_industries()` | `test_secondary_industries_come_from_the_taxonomy_not_a_literal` |
| NB-28 | `pyproject.toml:18` | Default `pytest` ran a network test against example.com | L | `addopts` deselects `network` | run shows `1 deselected` |
| NB-29 | `api/main.py:55` | `raw_jobs` / `*_params` unbounded and splatted into discovery | L | Size caps and `stop_at` validation | schema-level, exercised by the auth tests |
| NB-30 | `access/fetcher.py:247` | Camoufox took the page at `domcontentloaded` with no settle | L | Bounded `camoufox_settle_seconds` (3s) settle | manual (needs a browser) |
| NB-31 | `access/urlguard.py` (docstring) | Guard validated only the requested URL; a public host redirecting to 127.0.0.1 was fetched. The module docstring already claimed otherwise | H | `final_url_allowed()` re-checks the landed URL in `_build_page` and after Camoufox | `test_redirect_destination_is_revalidated`, `test_a_same_host_redirect_is_allowed` |
| NB-32 | `contacts/extraction.py:_OBFUSCATED_RE` | **Introduced by this pass and caught by a live run.** A bare " at " / "." matched prose: "celebration. Learn" → `celebr@ion.learn`, "patients. Read" → `p@ients.read` | C | Separators must be bracketed; bare forms rejected | `test_prose_never_becomes_an_email_address` (6 live strings), `test_bracketed_obfuscation_is_still_decoded` |
| NB-33 | `core/timeutils.py:16` | **Found by the live run.** `pd.NaT` is a truthy `datetime` subclass, so an undated LinkedIn posting reached `strftime` and killed the whole run | H | `pd.isna` guard at the top of `coerce_datetime` | `test_pandas_nat_is_not_treated_as_a_posting_date` |
| NB-34 | `api/main.py` | No inbound rate limiting on `/pipeline/run`, which spends money and starts large scrapes | M | In-process per-caller limiter, `NEXBASE_API_RATE_LIMIT` (60/min), applied to every route including `/unsubscribe` | `test_api_rate_limit_rejects_a_flood`, `test_rate_limit_is_per_caller` |
| NB-35 | `contacts/discovery.py` | Only conventional paths were tried; sites publishing their people page elsewhere were missed | M | Bounded sitemap lookup reusing `SiteEvidenceScanner._sitemap_urls`; same-host only, no crawl | `test_sitemap_contact_pages_stay_on_the_employer_domain` |
| NB-36 | `pipeline/runner.py:_ats_probe` | ATS rows carried no discovery tier or search intent | M | Probe's client industry passed as `search_industry` (intent), never `company_industry` | `test_ats_rows_carry_search_intent_not_company_industry` |
| NB-37 | `pipeline/runner.py` | Per-source outcome was logged but absent from `PipelineReport`, so a broken source was invisible to callers | M | `report.source_status` with SUCCESS/EMPTY/ERROR per source | `test_pipeline_report_exposes_per_source_status` |
| NB-38 | `dashboard/app.py` | Discovered company emails were not visible anywhere in the UI | L | Company-emails table with type, verification status and source URL | manual (Streamlit) |
| NB-39 | `scripts/live_smoke.py` | Report lacked contact-page emails, blocked-page counts, Scrapling usage and extraction completeness | L | All added; contact probing bounded and skippable | live run |
| NB-40 | `README.md` | `contacts_for_review_companies` and the size-band env aliases undocumented | L | Both documented, plus redirect revalidation | — |
| NB-44 | `db/schema.sql` evidence table | `runner._persist_company_emails` writes `raw_payload` and `/companies/{id}/emails` reads it, but the table never defined the column. With `db_strict=True` the first page-only email kills the run | C | `ALTER TABLE evidence ADD COLUMN IF NOT EXISTS raw_payload jsonb` | `test_evidence_table_defines_every_column_the_code_writes` |
| NB-45 | `contacts/extraction.py:_valid_person_name` | Live run on cardinalhealth.com stored "text message" ×3 and "email us" as named contacts; the mailto label was taken as a name | H | Reject UI vocabulary; require 2+ capitalised words | `test_call_to_action_text_is_never_a_person` |
| NB-46 | `config.py` | Size floor 11 vs client's 10; no target-industry allowlist; `excluded_sectors` empty although the client named IT/software/technology on 2026-09-14 | H | Floor 10; `target_client_industries` = Construction + the manufacturing family; exclusions set | `test_size_band_matches_the_client_spec`, `test_only_construction_and_manufacturing_are_targeted`, `test_only_targeted_industries_qualify`, `test_manufacturing_subtypes_still_qualify` |
| NB-47 | `config.py` | `contacts_for_review_companies=False` skipped contact discovery for NEEDS_REVIEW companies, which is most of them - the email-yield problem was structural, not extractive | H | Default flipped to True | `test_contact_discovery_runs_for_review_companies_by_default` |
| NB-48 | `discovery/board_scrapers.py` | Adapters parsed results cards only. SimplyHired publishes 0 descriptions there, so its 20 jobs could never yield an email | H | Bounded detail-page fetch fills missing descriptions and harvests their emails | `test_board_adapters_fetch_detail_pages_for_missing_descriptions`, `test_detail_page_fetching_is_budgeted` |
| NB-49 | `contacts/discovery.py` | `discover_subdomains()` existed but was reachable only via `WebSizeResolver`, disabled by default - dead in every run. No public-directory stage at all | H | Subdomain stage (CT-sourced, ranked, bounded) and directory stage wired into contact discovery | `test_contact_discovery_has_a_subdomain_and_directory_stage` |
| NB-50 | `pipeline/freshness.py` | No full-time filter and no US gate; the client excludes contract, part-time, temporary, contract-to-hire, freelance, independent-contractor and seasonal roles, and targets the US only | H | `employment_verdict()` and `is_us_location()` applied before age. An unstated type is accepted, not guessed | `test_employment_type_rules` (12 cases), `test_us_only_gate` (6), `test_non_full_time_job_is_rejected_before_age`, `test_non_us_job_is_rejected` |
| NB-51 | `discovery/board_scrapers.py` | SimplyHired cards carry no date and no employment type either, so 17/20 jobs were dropped for MISSING_POSTING_DATE and the full-time rule had nothing to judge | H | Detail fetch also reads `datePosted` and `employmentType` from the posting's JSON-LD. Live: dated 3→20, described 0→20, 15 employment types, 1 email found | `test_detail_page_supplies_date_and_employment_type` |
| NB-52 | `pipeline/freshness.py` | **Introduced this round.** schema.org spells types `PART_TIME`; the token list only had "part time"/"part-time", so `PART_TIME` was silently accepted as full-time | H | Fold `_`, `-`, `/` to spaces before matching | `test_schema_org_employment_types` (7 cases) |
| NB-53 | `access/ratelimit.py:FallbackBudget` | Measured on hamiltonparker.com: **17 of 20 contact pages lost to `HOST_COOLDOWN`**. The first Camoufox rescue stamped a 300s lock on the host, so every later blocked page was denied and the company's site abandoned - one rescued page per five minutes against the 10-20 contact discovery needs | H | Per-host allowance (`camoufox_host_max_fallbacks`, default 6) before the cooldown starts; the run-wide budget still caps cost. Live: camoufox rescues 1 → 6, cooldown losses 17 → 13 | `test_fallback_budget_enforces_host_cooldown_after_the_allowance`, `test_fallback_budget_rescues_several_pages_on_one_host`, `test_run_budget_still_caps_total_cost` |
| NB-54 | `access/urlguard.py`, `access/fetcher.py` | Two dead company domains produced **40 `URL_REJECTED` entries counted as SSRF blocks**, hiding the real reason in the stats | M | `HostUnresolved` subclass and a separate `unresolved_hosts` counter. Live: ssrf 40 → 0, unresolved 0 → 40 | `test_unresolvable_host_is_not_reported_as_a_security_block`, `test_a_private_address_is_still_an_ssrf_block` |
| NB-55 | `contacts/discovery.py` | All 20 conventional paths were tried against every company, dead or alive - 40 wasted attempts on two unresolvable hosts, and most paths 404 on live ones | M | Resolve the host once per company and skip on failure; read the homepage's own navigation for real contact/team URLs before guessing | `test_dead_company_site_is_checked_once_not_once_per_path`, `test_homepage_links_are_preferred_over_guessed_paths`, `test_homepage_link_harvest_is_bounded` |
| NB-56 | `contacts/discovery.py:_host_is_live` | **Introduced by NB-55 and caught in verification.** Caching a single failed DNS lookup skipped the whole company for the run. The two "dead" sites resolved normally minutes later - they were alive | H | Retry once before writing a company off; cache only the confirmed result | `test_a_transient_dns_failure_does_not_write_off_a_company`, `test_a_genuinely_dead_host_is_cached_after_the_retry` |
| NB-57 | (no change) | The 21 apparent extractor misses were all `placeholder="email@example.com"` form attributes. The extractor reads text, mailto and data-email, not attributes - it was correct | — | None. Pinned so nobody "fixes" it into reading placeholders | `test_form_placeholder_is_never_read_as_an_email` |
| NB-58 | `email/discovery.py`, `core/enums.py`, `pipeline/runner.py` | A lead carried only a flat `emails` list - no status, no provenance, no split between a named person and a shared mailbox, so the client's output spec could not be met | H | `EmailStatus` enum, `ObservedEmail` with source URL/portal/stage, and `email_status` + `observed_emails` + `named_contact_emails` + `role_mailboxes` + `portals_searched` + `discovery_stages` on the lead | `test_email_status_reflects_what_was_observed`, `test_observed_emails_carry_provenance`, `test_role_mailbox_is_never_attributed_to_a_person`, `test_a_lead_with_only_a_role_mailbox_is_still_a_lead`, `test_lead_exposes_the_fields_the_output_spec_requires` |
| NB-59 | `db/schema.sql`, `db/repository.py`, `pipeline/runner.py`, `api/main.py`, `dashboard/app.py` | NEEDS_REVIEW companies were persisted but invisible: no queue, no decision path, no history. Unknown employee size is the usual reason to land there, so real prospects simply disappeared | H | `review_queue` + `review_history` tables (RLS-covered), repository CRUD with transition history, runner files every NEEDS_REVIEW company with its flags and evidence, `/reviews` + `/reviews/{id}/decide` + `/reviews/{id}/history`, and a dashboard tab with approve/reject/keep-pending | `test_needs_review_company_is_filed_with_its_evidence`, `test_an_already_decided_company_is_not_reopened`, `test_review_decisions_are_recorded`, `test_repository_decide_review_writes_history`, `test_review_tables_exist_and_are_rls_protected` |
| NB-60 | `api/main.py`, `dashboard/app.py`, `.env` | The employee band was config-only, with no operator control, and `.env` still pinned the floor to **11** so the client's 10-200 default was not actually in force at runtime | H | `SizeRange` on the run request (per-run `model_copy`, no leak), `/config/size-range`, a sidebar slider in Streamlit, and the `.env` floor corrected to 10 | `test_size_range_flows_from_the_api_without_leaking`, `test_size_range_rejects_an_inverted_band`, `test_effective_size_band_matches_the_client_spec`, `test_the_four_size_cases_are_handled_separately`, `test_a_custom_band_changes_the_verdict_without_touching_evidence`, `test_dashboard_exposes_a_size_slider_and_review_tab` |
| NB-61 | `pipeline/runner.py`, `config.py` | **Introduced by NB-47 and caught in live validation.** Turning contact discovery on for NEEDS_REVIEW companies had no ceiling: a six-query run produced 628 companies and completed 3 discoveries in 25 minutes. The run would never have finished | H | `contacts_max_companies_per_run` (40) plus QUALIFIED-before-NEEDS_REVIEW ordering; skipped companies keep their jobs and evidence and are flagged `CONTACTS_NOT_ATTEMPTED` | live run: 12/12 budget honoured, 7 leads with emails |
| NB-62 | `db/schema.sql:398` (pre-fix) | **Introduced by NB-59.** The `updated_at` trigger loop named `review_queue` at line 398, but the table was created at 419. `DROP TRIGGER IF EXISTS ... ON review_queue` raises when the *table* is missing - IF EXISTS covers the trigger, not the table - so `apply-schema` aborted and everything after it, including RLS and `evidence.raw_payload`, never ran | C | Review tables moved above the trigger block; order is now tables → triggers → RLS | `test_every_table_is_created_before_it_is_referenced` (generic, checks every DO-block reference), `test_review_tables_precede_the_trigger_and_rls_blocks`, `test_schema_statements_parse_and_are_balanced` |
| NB-63 | `db/repository.py:health_check` | The table list drifted behind the schema: 15 tables declared, 13 checked, so `db health` reported "schema_applied: true" while `review_queue` and `review_history` went unverified | M | List extended to 15; a static test now compares it against every `CREATE TABLE` in `schema.sql` | `test_health_check_covers_every_table_in_the_schema` |
| H6 | `config.py`, `cli.py`, `dashboard/app.py` | 10-200 was a config value with no operator control; `min_employees`/`max_employees` were not accepted spellings | H | Band fully configurable (three env spellings), 10-200 as defaults only; CLI `--min-employees/--max-employees` with an inverted-band guard; dashboard slider POSTs `size_range` to `/pipeline/run`; a test parses `evaluate_size` to prove no literal band survives in the gate | `test_every_size_spelling_binds`, `test_size_defaults_are_ten_and_two_hundred`, `test_no_size_number_is_hardcoded_in_qualification_logic`, `test_cli_exposes_the_band_per_run`, `test_dashboard_sends_the_selected_band_to_the_api` |
| H6b/H14 | `contacts/discovery.py`, `pipeline/runner.py` | The standalone size resolver stayed off (0 explicit statements over 24 pages), so unknown size was permanent. Separately, size evidence could be written without a `record_id` | H | Contact discovery already reads ~30 pages per company; those pages are now scanned for an explicit headcount, the company re-scored, and the evidence persisted against its `record_id`. No extra fetch, nothing inferred | `test_size_found_on_a_contact_page_is_recorded`, `test_a_page_without_a_headcount_records_nothing`, `test_free_size_evidence_clears_review_and_is_persisted` |
| H5 | `discovery/ats_discovery.py:resolve_company_sites` | `find_company(name, limit=1)` took the top fuzzy hit with **no verification**, so "Summit Construction" could be given a same-named firm's domain two states away | H | Five candidates scored on name overlap (`name_similarity`, threshold 0.75) and corroborated by the posting's location; the match and its score travel with the job as `ats_company_match` | `test_ats_rejects_a_same_named_company_that_is_not_a_match`, `test_ats_accepts_a_verified_match_with_provenance`, `test_ats_prefers_the_location_corroborated_candidate`, `test_ats_match_provenance_reaches_the_job`, plus 7 similarity cases |
| H4 | `pipeline/runner.py:_resolve_domains`, `config.py` | Budget of 25/run in arbitrary order, so a 600-company run resolved domains for the first 25 it happened to reach | H | Budget raised to 150 and made priority-based: QUALIFIED before NEEDS_REVIEW, then hiring intensity, then freshness. Skipped count reported | `test_domain_budget_is_spent_on_the_best_companies_first`, `test_domain_budget_reports_what_it_skipped`, `test_domain_budget_default_is_no_longer_a_bottleneck` |
| C2, H7 | `config.py`, `pipeline/qualification.py` | **DEFERRED — superseded by the client spec of 2026-09-14**, which states "Construction and Manufacturing companies only" (line 23) and excludes "IT, software, or technology companies" (line 96). Exclusions *were* client-supplied and the allowlist *is* the requirement. Restoring 80/20 exploration would spend ~20% of each run on industries that can never qualify. Confirmed with the operator before deferring | — | No change | `test_only_construction_and_manufacturing_are_targeted`, `test_only_targeted_industries_qualify` |

## Not fixed — deliberately

| ID | Issue | Why |
|---|---|---|
| NB-41 | `cli.py`, `export.py`, `flows/`, `dashboard/` at 0% coverage | Out of scope for this pass; no defect verified in them. |
| NB-42 | SimplyHired returns 0 job descriptions | Description email extraction cannot fire for it. Reported, not worked around. |
| ~~NB-43~~ | ~~RLS DDL not applied~~ | **RESOLVED 2026-09-15**: schema applied to live Supabase; all 15 tables reachable, RLS enabled and forced. |

## Decided by the client, 2026-09-14

| Question | Decision | Implemented |
|---|---|---|
| Prefect weekday cron | **Remove entirely** | `serve(...)` deleted; a test asserts no module declares `cron=` or calls `.serve(` |
| Paid ZeroBounce verification | **Behind the approval gate** | `verify_emails_requires_approval`, default true |
| Page-only company emails | **Keep evidence-table storage** | `evidence` rows with type and provenance |

## Baseline vs. after

| | Before | After |
|---|---|---|
| pytest | 427 passed, 2 warnings (1 test hit the network) | **649 passed, 1 deselected**, no network |
| pip check | clean | clean |
| compileall | clean | clean |
| coverage | 71% (5039 stmts) | **75%** |
| `api/main.py` coverage | 0% | 61% |

No pre-existing failures were present at baseline, so none were hidden or
carried forward.
